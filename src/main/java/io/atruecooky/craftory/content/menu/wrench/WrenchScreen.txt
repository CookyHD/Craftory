package io.atruecooky.craftory.content.menu.wrench;

import net.minecraft.client.gui.GuiGraphics;
import net.minecraft.client.gui.screens.inventory.AbstractContainerScreen;
import net.minecraft.network.chat.Component;
import net.minecraft.world.entity.player.Inventory;

public class WrenchScreen extends AbstractContainerScreen<WrenchMenu> {

	//private static final int textureHight = 128;
	//private static final int textureWidth = 128;

	public double mouseX = 0;
	public double mouseY = 0;

	public WrenchScreen(WrenchMenu menu, Inventory inventory, Component title) {
		super(menu, inventory, title);
	}

	@Override
	public void render(GuiGraphics guiGraphics, int mouseX, int mouseY, float partialTick) {
		this.mouseX = mouseX;
		this.mouseY = mouseY;
	}

	@Override
	protected void renderBg(GuiGraphics guiGraphics, float partialTick, int mouseX, int mouseY) {
		//NOTHING LMAO
	}

	// private int getSelected() {

	// 	float angle = getAngle();
	// 	float distance = getDistance();
	// 	float scale = getGuiScale();

	// 	if (distance > 4 * scale) {
	// 		if (angle > 270) return 2;
	// 		if (angle > 180) return 3;
	// 		if (angle > 90) return 4;
	// 		if (angle > 0) return 1;
	// 	}

	// 	return 0;
	// }

	// private float getAngle() {

	// 	final Vector2d UP = new Vector2d(-1d,-1d);
		
	// 	Vector2d Middle = new Vector2d(this.width/2d,this.height/2d);
	// 	Vector2d Mouse = new Vector2d(this.mouseX,this.mouseY);
	// 	Vector2d Pos = Mouse.sub(Middle);

	// 	Pos.normalize();

	// 	double angle = Math.toDegrees(UP.angle(Pos));

	// 	if (angle < 0) return 360f - (float) Math.abs(angle);
	// 	else return (float) angle;
	// }

	// private float getDistance() {
	// 	Vector2d Middle = new Vector2d(this.width/2d,this.height/2d);
	// 	Vector2d Mouse = new Vector2d(this.mouseX,this.mouseY);
	// 	return (float) Middle.distance(Mouse);
	// }

	// private static float getGuiScale() {
	// 	Window window = Minecraft.getInstance().getWindow();
	// 	return (float) window.getGuiScale();
	// }
}
